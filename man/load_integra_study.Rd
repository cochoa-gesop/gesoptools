% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/integra_export.R
\name{load_integra_study}
\alias{load_integra_study}
\title{Carga un estudio de Integra y devuelve el data frame unificado listo para
consumir directamente por el Shiny, sin transformaciones adicionales.}
\usage{
load_integra_study(
  study_id,
  db_config,
  valid_states = c(1, 5, 6, 9),
  idioma = 1L,
  clean_html = TRUE,
  clean_instructions = TRUE,
  ns_nc_recode = TRUE
)
}
\arguments{
\item{study_id}{Identificador del estudio (p.ej. "1824_POL_ARAGON")}

\item{db_config}{Lista con host, port, user, password, dbname}

\item{valid_states}{Codigos ESTADO validos (por defecto c(1,5,6,9))}

\item{idioma}{Codigo de idioma en variablescodigos (1 = principal)}

\item{clean_html}{Si TRUE, elimina entidades HTML de las etiquetas}

\item{clean_instructions}{Si TRUE, elimina textos de instruccion de entrevistador}

\item{ns_nc_recode}{Si TRUE, unifica variantes de NS/NC a "NS" y "NC"}
}
\value{
Lista con:
$data     data frame con el contrato unificado
$metadata lista con $variables (nomvar, label) y
$codebook  (nomvar, value, label, label_short)
}
\description{
El data frame de salida sigue el contrato unificado:
\itemize{
\item Variables categoricas -> factor con etiquetas de texto como levels
\item Variables numericas   -> numeric
\item Variables de texto    -> character (incluyendo multiples con ";")
\item Columnas admin:       REGISTRO (character), FECHAFIN (Date),
POND (numeric, 1 por defecto),
ESTUDI, MES, DATA
}
}
